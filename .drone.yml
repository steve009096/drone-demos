workspace:
  base: /go
  path: src/github.com/drone-demos/drone-with-go

pipeline:
  test:
    image: golang:latest
    commands:
      - pwd
      - ll
      - echo test9
      # - go vet
      # - go test -v -cover
  build:
    image: golang:latest
    commands:
      - pwd
      - ll
      - echo build-32
      # - go build
